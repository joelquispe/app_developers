import 'package:flutter/material.dart';

class CouseBootstrap extends StatefulWidget {
  static String route = "/coursebootstrap";
  CouseBootstrap({Key? key}) : super(key: key);

  @override
  State<CouseBootstrap> createState() => _CouseBootstrapState();
}

class _CouseBootstrapState extends State<CouseBootstrap> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}